import * as React from "react";

// components
import Banner from "../Banner";

const Home = () => {
    return (
        <section>
            <Banner />
        </section>
    );
};

export default Home;
