import React from "react";

const FilterRange = ({ name }) => {
	return (
		<div className="filter-range">
			<h5 className="text-1">{name} range</h5>
		</div>
	);
};

export default FilterRange;
